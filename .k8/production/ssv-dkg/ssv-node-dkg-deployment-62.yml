---
apiVersion: v62
kind: Service
metadata:
  name: ssv-dkg-62-svc
  namespace: REPLACE_NAMESPACE
  labels:
    app: ssv-dkg-62
spec:
  type: ClusterIP
  ports:
    - port: 3062
      protocol: TCP
      targetPort: 3062
      name: port-3062
  selector:
    app: ssv-dkg-62
---
apiVersion: REPLACE_API_VERSION
kind: Deployment
metadata:
  labels:
    app: ssv-dkg-62
  name: ssv-dkg-62
  namespace: REPLACE_NAMESPACE
spec:
  replicas: REPLACE_REPLICAS
  selector:
    matchLabels:
      app: ssv-dkg-62
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: ssv-dkg-62
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/role
                    operator: In
                    values:
                      - ssv-main
                  - key: beta.kubernetes.io/instance-type
                    operator: In
                    values:
                      - m5a.4xlarge
      containers:
      - name: ssv-dkg-62
        image: REPLACE_DOCKER_REPO:REPLACE_IMAGETAG
        imagePullPolicy: Always
        resources:
          limits:
            cpu: REPLACE_NODES_CPU_LIMIT
            memory: REPLACE_NODES_MEM_LIMIT
        ports:
        - containerPort: 3062
        command: ["/app", "start-operator", "--configPath=/data/operator.yaml", "--privKeyPassword=/data/password", "--privKey=/data/encrypted_private_key_op62.json"]
        volumeMounts:
        - name: secrets-file
          mountPath: "/data/encrypted_private_key_op62.json"
          subPath: encrypted_private_key_op62.json
        - name: ssv-dkg-62-cm
          mountPath: /data/password
          subPath: password
        - name: ssv-dkg-62-cm
          mountPath: /data/operator.yaml
          subPath: operator.yaml
      volumes:
        - name: secrets-file
          secret:
            secretName: ssv-dkg-62
        - name: ssv-dkg-62-cm
          configMap:
            name: ssv-dkg-62-cm
      tolerations:
        - effect: NoSchedule
          key: kubernetes.io/role
          operator: Exists
      hostNetwork: true
