---
apiVersion: v1
kind: Service
metadata:
  name: ssv-dkg-72-svc
  namespace: REPLACE_NAMESPACE
  labels:
    app: ssv-dkg-72
spec:
  type: ClusterIP
  ports:
    - port: 3072
      protocol: TCP
      targetPort: 3072
      name: port-3072
  selector:
    app: ssv-dkg-72
---
apiVersion: REPLACE_API_VERSION
kind: Deployment
metadata:
  labels:
    app: ssv-dkg-72
  name: ssv-dkg-72
  namespace: REPLACE_NAMESPACE
spec:
  replicas: REPLACE_REPLICAS
  selector:
    matchLabels:
      app: ssv-dkg-72
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: ssv-dkg-72
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/role
                    operator: In
                    values:
                      - ssv-main
                  - key: beta.kubernetes.io/instance-type
                    operator: In
                    values:
                      - m5a.4xlarge
      containers:
      - name: ssv-dkg-72
        image: REPLACE_DOCKER_REPO:REPLACE_IMAGETAG
        imagePullPolicy: Always
        resources:
          limits:
            cpu: REPLACE_NODES_CPU_LIMIT
            memory: REPLACE_NODES_MEM_LIMIT
        ports:
        - containerPort: 3072
        command: ["/bin/ssv-dkg", "start-operator", "--configPath=/data/config", "--privKeyPassword=/data/password", "--privKey=/data/encrypted_private_key_op72.json"]
        volumeMounts:
        - name: secrets-file
          mountPath: "/data/encrypted_private_key_op72.json"
          subPath: encrypted_private_key_op72.json
        - name: ssv-dkg-72-cm
          mountPath: /data/password
          subPath: password
        - name: ssv-dkg-72-cm
          mountPath: /data/config/config.yaml
          subPath: operator.yaml
      imagePullSecrets:
        - name: ecr-repo
      volumes:
        - name: secrets-file
          secret:
            secretName: ssv-dkg-72
        - name: ssv-dkg-72-cm
          configMap:
            name: ssv-dkg-72-cm
      tolerations:
        - effect: NoSchedule
          key: kubernetes.io/role
          operator: Exists
      hostNetwork: true
